import 'package:flutter/material.dart';

class MobileHomeSection extends StatelessWidget {
  const MobileHomeSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Home Section'));
  }
}
